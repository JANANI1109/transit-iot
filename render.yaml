services:
  - type: web
    name: transit-iot-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TICKET_SECRET
        generateValue: true
    autoDeploy: true
